name: Tech Knowledge Hook

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Sync dependencies
        run: uv sync --frozen

      - name: Run Tech Knowledge Hook
        env:
          ZAI_API_KEY: ${{ secrets.ZAI_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          DISCORD_WEBHOOK_TECH_KNOWLEDGE: ${{ secrets.DISCORD_WEBHOOK_TECH_KNOWLEDGE }}
          ENV: production
        run: uv run python -m tech_knowledge
