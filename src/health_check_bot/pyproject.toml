[project]
name = "health-check-bot"
version = "0.1.0"
description = "Simple HTTP Health Check Bot"
requires-python = ">=3.11"
dependencies = [
    "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
